export const ballCount = 4
export const ballRadius = 10
export const ballDiameter = ballRadius * 2
export const ballContainer = ballDiameter + ballRadius / 2

export const flaskWidth = ballContainer
export const flaskHeight = ballContainer * ballCount

export const containerGap = 20
